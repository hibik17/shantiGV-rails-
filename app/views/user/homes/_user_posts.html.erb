<div class="ui four cards">
  <% if @posts.count > 1 %>
    <% @posts.each do |post| %>
      <div class="card">
        <div class="image">
          <%= attachment_image_tag post, :image, fallback: "noimage.png"%>
        </div>
        <ul>
          <li class="ui list"><p class="ui label">title</p> <%= post.title %></li>
          <li class="ui list"><p class="ui label">content</p><%= post.content %></li>
        </ul>
      </div>
    <% end %>
  <% else %>
    <p style="color: red;">まだ投稿はありません。初めての投稿をしてみましょう!</p>
    <%= link_to "投稿をしてみよう!", new_user_post_path, class: 'ui button orange' %>
  <% end %>
</div>
