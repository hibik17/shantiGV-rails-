<% if current_user.following?(user) %>
  <%= link_to "Unfollow", user_relationship_path(user.id), method: :delete, class: 'ui button violet' %>
<% else %>
  <%= link_to "Follow", user_relationships_path(follwer_id: current_user.id, followed_id: user.id), method: :post, class: 'ui button violet' %>
<% end %>