<%= form_with model: @post, url: user_posts_path, local:true do |f| %>
  <%= f.label :genre %>
  <%= f.select :genre_id, Genre.all, :name, :id %>

  <%= f.label :country %>
  <%= soo
  %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :content %>
  <%= f.text_area :content %>

  <%= f.submit "作成" %>
<% end %>